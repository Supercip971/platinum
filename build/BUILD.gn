config("compiler_defaults") {
  if (current_os == "linux") {
    cflags = [ "-pthread" ]
    cflags_cc = [ "-std=c++2b" ]
  }
}

config("executable_ldconfig") {
  ldflags = [
    "-Wl,-rpath=\$ORIGIN/",
    "-Wl,-rpath-link=",
  ]
}
